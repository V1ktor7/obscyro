# Core service deps — the service boots and /health + mechanistic /simulate work
# with only these. Heavy DL deps are in requirements-ml.txt and lazy-imported.
fastapi==0.111.0
uvicorn[standard]==0.30.1
pydantic==2.7.1
# Core, not ML extras. The crisis layer needs both, and the ML install is
# deliberately allowed to fail (`|| echo "ml extras skipped"`), so anything the
# engine imports must live here or it is one broken pin away from vanishing.
# That is not hypothetical: `pytorch-forecasting==1.0.0` does not exist for
# Python 3.11, so the whole ML block has been skipping silently — which is how
# numpy went missing.
#
# networkx is imported lazily inside NetworkxBackend, so leaving it out builds a
# healthy image whose /health passes and whose first simulation raises
# ModuleNotFound: a deploy that looks successful until somebody uses it.
networkx==3.5
# Pinned to the same range requirements-ml.txt asks for, so installing the ML
# stack later does not resolve a second, different numpy underneath it.
numpy>=1.26,<2.0

# The lab. These are core, not extras, and the reason is written above: the ML
# block installs with `|| echo "ml extras skipped"`, so anything the product
# actually depends on must never live there. A lab that silently vanishes on a
# deploy is worse than no lab.
#
# Pinned to a 1.x line that exists for Python 3.11 — the trap that made
# pytorch-forecasting skip for months was a version that does not.
pandas>=2.2,<3.0
scikit-learn>=1.5,<2.0
